{{!-- Carte de blessure grave (0 PV) --}}
<div class="brigandyne-40k chat-card warp-card peril">
  <header class="card-header">
    <h3 class="card-title"><i class="fa-solid fa-heart-crack"></i> Blessure grave — {{actorName}}</h3>
  </header>
  <div class="card-result">
    <span class="roll-vs">{{location}}</span>
    <span class="roll-total" style="font-size:1.3rem">{{name}}</span>
  </div>
  <div class="card-meta"><div class="meta-line"><span class="meta-value">{{{effect}}}</span></div></div>
</div>
